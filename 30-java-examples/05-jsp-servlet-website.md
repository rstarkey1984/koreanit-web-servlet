# Tomcat + JSP/Servet 으로 동작하는 `<subdomain>.localhost` 사이트 만들기

## 🧩 실습 / 과제

## 1. 서버에서 `/www/var/<subdomain>.localhost` 웹 어플리케이션 디렉터리 생성 및 설정 

## 2. Tomcat `/etc/tomcat10/server.xml` 에서 `<subdomain>.localhost` 가상호스트 구성

## 3. `/www/var/<subdomain>.localhost` 에서 JSP/Servlet 코드 작성

## 4. `VSCode` 에서 빌드 & Tomcat 재시작

## 5. http://`<subdomain>`.localhost